// googleAdsClient.ts
// DEPRECATED — this app was pivoted from Google Ads to Meta Ads (Facebook /
// Instagram). The real-account "traficker" integration now lives in
// metaAdsClient.ts and is consumed by the /api/meta-account/* endpoints in
// server.ts. This file is intentionally left empty (no exports) so it does
// not interfere with compilation; it is no longer imported anywhere.
export {};
